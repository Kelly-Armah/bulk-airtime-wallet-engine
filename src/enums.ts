export enum TransactionType {
  AIRTIME = 'airtime',
  CREDIT = 'credit',
  DEBIT = 'debit',
}

export enum TransactionStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SUCCESSFUL = 'success',
  REJECTED = 'rejected',
  FAILED = 'failed',
}

export enum Network {
  MTN = 'MTN',
  TELECEL = 'Telecel',
  AT = 'AT',
}